version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  # No bundler entry: upstream's Jekyll documentation site lived at /docs and
  # went with the rest of their docs during the rebrand (f7a8d4a7). Dependabot
  # kept scanning the missing directory and failing every week.
